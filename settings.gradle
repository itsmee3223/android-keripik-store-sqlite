pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        maven { url 'https://jitpack.io' }
        jcenter() //Keep jcenter() in the last
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        maven { url 'https://jitpack.io' }
        jcenter() //Keep jcenter() in the last
    }
}
rootProject.name = "Keripik Store"
include ':app'
